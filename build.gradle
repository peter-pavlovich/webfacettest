apply plugin: 'war'

//sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile "struts:struts:1.2.9"
    compile "org.apache.tiles:tiles-jsp:2.0.3"
    testCompile group: 'junit', name: 'junit', version: '4.11'
}